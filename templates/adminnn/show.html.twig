{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h1>Admin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ admin.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ admin.password }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ admin.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ admin.lastName }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ admin.phone }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ admin.roles ? admin.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Department</th>
                <td>{{ admin.department }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adminnn_index') }}">back to list</a>

    <a href="{{ path('app_adminnn_edit', {'id': admin.id}) }}">edit</a>

    {{ include('adminnn/_delete_form.html.twig') }}
{% endblock %}
